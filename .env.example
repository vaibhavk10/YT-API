# Server Configuration
PORT=3000
BASE_URL=http://localhost:3000

# API Configuration
API_NAME=YouTube Download API

# File Management
# Files older than this (in milliseconds) will be cleaned up
# Default: 3600000 (1 hour)
FILE_CLEANUP_AGE=3600000

# Optional: Cookies file path (relative to api folder)
# If you have cookies.txt, place it in the api/ folder
# The server will automatically use it if it exists
